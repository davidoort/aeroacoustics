function lambda_up = get_lambda_up(F,r,pitch,rotor)

%F = Prandtl tip loss
%r = radial position of blade
%pitch of blade element wrt to rotor plane in rad
%rotor struct (either 1 or 2)

sigma = rotor.solidity;
cl_a = rotor.cl_alpha;

lambda_up = sqrt(()^2)



end